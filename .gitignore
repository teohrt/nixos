# Claude Code session files (settings, context, todos)
.claude*

# nix-direnv cache — cached devShell environment so re-eval is fast
.direnv/

# Generated symlink to /nix/store by git-hooks.nix shellHook — hook config is defined in flake.nix
.pre-commit-config.yaml

# Symlink to /nix/store created by nixos-rebuild build / nix build
result

# Generated by devShell — contains nix store paths for Hyprland lua stubs
.luarc.json

